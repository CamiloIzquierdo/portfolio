import { useRef } from "react";

export const Home: React.FC = () => {
   /*  function scrollToComponent(componentRef) {
        componentRef.current.scrollIntoView({ behavior: "smooth" });
    }
    const projectRef = useRef(null);
    const aboutMeRef = useRef(null);
     */
    return (

    )
}

